# /workflow

Create or edit user-defined workflows in ContxOS.

## Execute

**Create new workflow:**
```bash
contxos :: execute '{"intent": "capture", "branch": "Create Workflow"}'
```

**Edit existing workflow:**
```bash
contxos :: execute '{"intent": "capture", "branch": "Edit Workflow"}'
```

ARGUMENTS: $ARGUMENTS
